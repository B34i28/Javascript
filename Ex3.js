var name = "Gull Sher Khan";

console.log(name.toLowerCase());

console.log(name.toUpperCase());

var first_char = name.slice(0,1).toUpperCase();
var other_char = name.slice(1).toLowerCase();
console.log(first_char + other_char);